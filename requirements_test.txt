# Test harness. pytest-homeassistant-custom-component pins the Home Assistant
# version the suite runs against: 0.13.364 pins 2026.9.1, the deployment target.
# qolsys-controller is not listed on purpose - it is vendored under
# custom_components/qolsys_panel/vendor/, so the tests cannot run against a
# different version of the library than the integration ships.
pytest-homeassistant-custom-component==0.13.364
mypy==2.3.1
GitPython==3.1.58
